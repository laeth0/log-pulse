# Log Ingestion and Query Service

> [!IMPORTANT]
> The previously tracked local database credential must be treated as exposed.
> Rotate it anywhere it was reused before running this service. The real `.env`
> file is intentionally ignored; copy `.env.example` and use local credentials.

This README will be expanded with verified setup, API, retention, and performance
evidence as the implementation tasks reach their corresponding verification gates.
